#!/bin/sh
set -e

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload || true
    systemctl enable sendspin-service || true
    systemctl restart sendspin-service || true
fi

exit 0
